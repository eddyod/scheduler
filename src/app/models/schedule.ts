export class Schedule  {
  start: string;
  end: string;
  school_id: number;
  teacher_id: number;
  createdBy: number;
  pay_rate: number;

  constructor(start, end, school_id, teacher_id, createdBy, pay_rate) {}

}
