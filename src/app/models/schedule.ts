export class Schedule  {
  start: string;
  end: string;
  location_id: number;
  employee_id: number;
  created_id: number;
  pay_rate: number;

  constructor(start, end, location_id, employee_id, created_id, pay_rate) {}

}
